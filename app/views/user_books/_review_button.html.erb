<% if user_book.review.present? %>
  <%= link_to 'Edit Review', edit_user_book_review_path(user_book), class: 'btn btn-primary' %>
<% else %>
  <%= link_to 'Add Review', new_user_book_review_path(user_book), class: 'btn btn-secondary' %>
<% end %>